import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-issue-ion-item-sliding',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
